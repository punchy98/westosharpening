---
interface Props {
  icon: string;
  title: string;
}

const { icon, title } = Astro.props;
---

<article class="service-card">
  <h3><span aria-hidden="true">{icon}</span> {title}</h3>
  <slot />
</article>
